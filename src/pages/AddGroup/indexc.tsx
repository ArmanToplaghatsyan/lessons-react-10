import React from 'react';
import './style.scss';

export const AddGroup: React.FC = React.memo((): JSX.Element => {
  return (
    <div className="addgroup">
      <h2>Add Group</h2>
    </div>
  );
});
